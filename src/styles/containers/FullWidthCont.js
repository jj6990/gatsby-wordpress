import styled from 'styled-components';
import device from '../Device';

const FullWidthCont = styled.div` 
        max-width: 1440px;
        margin: 1rem auto;
        width: 100%; 
        box-sizing: border-box;
        position: relative;
        padding: 0 15px;
`

export default FullWidthCont;
import styled from 'styled-components';
import device from '../Device';

const FullWidthCont = styled.section` 
        max-width: 1440px;
        margin: 1rem auto;
        width: 100%; 
        box-sizing: border-box;
`

export default FullWidthCont;